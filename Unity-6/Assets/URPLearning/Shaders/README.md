# Unity 6 Shaders

本目录已有：`01_UnlitColor`、`Includes/URPLearningCommon.hlsl`、`Hidden_FullScreenTint`（采样 `_BlitTexture`）。

`02`–`06` 与根目录 [Assets/URPLearning/Shaders](../../../../Assets/URPLearning/Shaders) 相同，可直接拷进来。

Feature 必须用本版本的 `Scripts/FullScreenTintFeature.cs`（`RecordRenderGraph`），不要用 2022 的 `Execute` 脚本。
